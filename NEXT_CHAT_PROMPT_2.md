# LifeCompass 次チャット引き継ぎプロンプト

## プロジェクト概要

KENZOが開発するFIRE資産シミュレーター「LifeCompass」のブログコンテンツ制作・サイト構築の継続作業です。

- **シミュレーター**: https://freenough-lifecompass.vercel.app/simulator
- **ブログ**: https://freenough-lifecompass.vercel.app/blog
- **note**: https://note.com/freenough
- **開発環境**: Windows / PowerShell / Next.js / Vercel
- **デプロイコマンド**: `git add -A` → `git commit` → `vercel --prod`

---

## 現在の進捗状態

### ✅ ブログ記事(3本掲載済み)

| 優先度 | タイトル | 状態 |
|---|---|---|
| 1 | 4%ルールは日本でも通用する? | ✅掲載済み(修正後数値に更新済み) |
| 2 | iDeCoとNISA、どう組み合わせる? | ✅掲載済み |
| 3 | モンテカルロシミュレーションとは? | ✅掲載済み(画像サンプル4枚も作成・配置済み) |
| **4** | **FIRE達成に必要な資産額の計算方法** | 📝**次はこれ** |
| 5 | 退職金・年金を含めたセミリタイア計画 | 📝未着手 |
| 6 | 新NISA満額後は何に投資すべきか | 📝未着手 |

**モンテカルロ記事の設計上の工夫(次の記事にも応用できる考え方):**
- 4%ルール記事との差別化のため、「田中さんのポートフォリオ比較」ではなく「μ・σを直接コントロールした教育用ケースA/B/C」を使う構成にした
- 理由:①記事の主役はモンテカルロ法自体であるべき、②資産配分の話にすり替わるのを避ける、③LifeCompassの「μ・σを自由に設定できる」という機能はSEO的にも独自性が高い
- 教育用ケースを使う際は「これは実際の資産配分比較ではなく教育用シナリオである」という断り書きを、前段の実データ(固定4%等)との混同を避けるために明記すること

### ✅ 確定済みの重要数値(田中さんプロフィール・修正後版)

- 42歳・会社員・配偶者40歳(年収200万)・総資産2,500万円・55歳退職
- **単純計算(固定4%)**:55歳7,367万円 → 90歳5,277万円
- **MC(1,000試行・破綻率25.4%は今後も変更しない方針で確定)**:
  - 65歳中央値5,527万円、p10 2,838万円、p90 1億565万円
  - 90歳最終資産:中央値3,099万円、p10 0万円、p90 1億6,500万円
  - 対策:支出10%削減→16.2%(-9.2pt)/ 退職2年延長→9.8%(-15.6pt)/ 余剰CF全額投資→20.6%(-4.8pt)
- **モンテカルロ記事の教育用ケース(μ/σ直接入力)**:
  - ケースA(μ5%σ8%):破綻率5.4%、p10 1,239万円、中央値9,446万円、p90 2.7億円
  - ケースB(μ6%σ12%):破綻率7.6%、p10 1,156万円、中央値1.6億円、p90 5.5億円
  - ケースC(μ7%σ16%):破綻率11.5%、p10 0万円、中央値2.0億円、p90 10.5億円
- **iDeCo受取比較**:一時金1,494万円(税138万) vs 年金(15年)1,540万円(税371万)。差46万円は税制差+運用継続効果の合計

**注意**:田中さんシリーズのMC数値(25.4%等)は**手動でμ・σを直接入力**して算出したものであり、後述するシミュレーターのσ計算バグ修正の影響を受けない(`mcStdDynamic`が発火しないため)ことを確認済み。記事の再検証は不要。

---

## 🔧 シミュレーター側の大規模バグ修正(このセッションの本題)

今セッションの後半は、ほぼすべてPF(ポートフォリオ)・利回り設定・MC設定まわりのUI再設計とバグ修正に費やした。Claude Codeに順次指示書を渡しながら進めており、**現在も対応中**。

### 発端

ブログ記事用にポートフォリオのμ/σ検証を依頼したところ、複数の深刻なバグが芋づる式に発覚した。

### 発覚したバグの系譜(時系列)

1. MC設定の標準偏差がPF側のσ計算値と同期しない/小数点入力不可 → 修正済み
2. 利回り直接入力が全口座集計に反映されない/初期PFが全世界株固定/step刻み不統一
3. PF口座の削除ボタンが1行以下で出ない(→最終的に「最低1行制約」自体が誤りと判明。0行まで削除可能にし、初期状態は空にする方針に変更)
4. **UI再設計**:PFカードのrW/rR直接入力欄を廃止しμ表示のみに、利回り設定側に「PF計算値を使う」トグルを追加、「取崩期は積立期と同じ利回りを使う」(利回り設定側)と「同じPFを使う」(PF側)の内部state同期を分離
5. σの計算方法自体に理論的な誤りを発見:
   - **重み付けが残高ベースでなくPF入力の有無になっていた**
   - **重みが静的で、積立による残高変化に追従していなかった**
   - → 「残高加重・年ごとに動的更新」が正しい仕様と確定
6. **最重要発見**:旧HTML版のコード調査で、モンテカルロのshock(乱数)は「全口座が同一のショックを受ける(相関=1)」という設計であることが判明。Next.js版(`montecarlo.ts`/`simulate.ts`)にも同じ設計が引き継がれていることを確認。これにより、σの計算バグは**表示だけでなく実際のシミュレーション結果(破綻率・p10/p90)に直接影響する**ことが確定
7. さらに検証を進めた結果、**相関=1のモデルでは「残高加重の単純線形和」が近似ではなく数学的な厳密解である**ことも確認(σ(aX+bY)=|a|σX+|b|σY when ρ=1)
8. **現在判明している最新バグ**:2口座以上に実際の資産配分がある場合、σの合成に**資産クラス間の相関マトリクス(本来は1口座内で複数資産クラスを混ぜる時にしか使うべきでないもの)が誤って流用されている**疑いが強い(数式的に検算し、相関0.1を使った場合の計算値とほぼ完全に一致することを確認済み)。単一口座のみに値がある場合は正しく計算されるため気づきにくかった

### 現在Claude Codeに渡っている最新の指示書

`FIX_sigma_still_incorrect_after_fix.md`(このチャットのアーティファクトとして作成済み。KENZOの手元にある想定)。内容:
- NISA単体・特定口座単体では正しいが、2口座以上で相関マトリクスが誤って混入している疑いを数式で立証
- `calcPortfolioMetrics`(本来は口座内の資産クラス合成専用)が口座間合成にも誤用されていないか確認するよう指示
- 修正後、CLAUDE.mdに「相関=1モデルでは加重線形和が厳密解」という説明文(確定文言あり)を残すよう指示

### 次チャットで最初に確認すべきこと

1. **このバグ(`FIX_sigma_still_incorrect_after_fix.md`)の修正がClaude Codeで完了したか**
2. 完了していれば、`node scripts/full-verify.js`で全fixture(山本・中村・田中・佐々木)がPASSしているか、特に**田中シリーズの数値が変化していないか**を確認
3. `npx tsc --noEmit`で型エラーがないか
4. 本番反映(`git add -A` → `git commit` → `vercel --prod`)

### 確定した設計方針(今後のセッションでも維持すること)

- モンテカルロのshockは口座横断で単一の値(相関=1)。**この設計自体は変更しない**(理由:実装コストと精度向上のバランス、かつ相関1の前提は保守的=安全側に倒れる近似のため許容できる)
- σは「各口座の実際の残高(積立期は残高+積立額、取崩期は残高のみ)を重みとした加重平均」。**重みは年ごとに動的に再計算**する
- μも同じ重み付けロジックで計算する(ただしμの計算はシミュレーションエンジン自体には影響しない、表示専用の値)
- 「PF計算値を使う」トグルのデフォルトは**OFF**(利回り設定に元から入っているデフォルト値で、PF未設定でも最初から意味のあるグラフが見える体験を維持するため)
- 上記の設計思想は、この一連の議論の理由づけごとCLAUDE.md(またはNext.js版の設計ドキュメント)に反映するようClaude Codeに依頼済み。次チャットで実際に反映されているか確認するとよい

---

## 🎨 保留中のUI提案:LPにブログへのリンクを追加

現状、LPからブログへの導線は画面右上のみ。noteリンクの近くに追加する方向で合意済み、3案のモックアップを提示し**案A(カード形式:アイコン+説明文つき、ブログを上・noteを下)を推奨として提示済み**。KENZOの最終承認・Claude Codeへの指示はまだ。

---

## 保存済みファイル一覧(outputs/、このチャットで作成)

- `ideco-nisa.md` — frontmatter付き掲載用(掲載済み)
- `4percent-rule.md` — 修正後数値版(掲載済み)
- `montecarlo-simulation.md` — frontmatter付き掲載用(掲載済み)
- `images/eyecatch-montecarlo.png`, `figure1-montecarlo-concept.png`, `figure2-percentile-fan-chart.png` — サンプル画像(配置済み)
- `FIX_mc_sigma_sync_and_decimal_input.md` — 対応済み
- `FIX_mc_yield_aggregate_sync_and_defaults.md` — 対応済み
- `FIX_pf_delete_button_and_empty_default.md` — 対応済み
- `FIX_yield_pf_ui_redesign.md` — 対応済み
- `FIX_leading_zero_and_sigma_toggle.md` — 対応済み
- `FIX_pf_aggregate_leaking_manual_value.md` — 対応済み
- `FIX_pf_aggregate_weighting_bug.md` — 対応済み
- `FIX_mc_setting_pf_value_mismatch.md` — 対応済み
- `FIX_mu_sigma_weighted_calculation_method.md` — 対応済み(実装プラン化・実行中)
- `FIX_tooltip_and_default_toggle.md` — 対応済み
- `FIX_sigma_still_incorrect_after_fix.md` — **★対応中・次チャットで進捗確認が必要**

---

## 注意事項(継続)

- 数値はすべてシミュレーターの実出力から取得。手計算値は使わない
- 記事内表現は「CSV」ではなく「LifeCompassで実際に試算した結果」
- CTAリンクのUTMパラメータ:`utm_source=blog&utm_medium=referral&utm_campaign=記事名`
- Windows/PowerShellのLF/CRLF警告は無害、無視してよい
- vimでのgit commit操作に不慣れなため、コミットメッセージは`git commit -m "..."`で直接渡す方法を優先すると良い
