# 実装指示書:破綻確率の表現を平均値+変動説明に統一

## 対象・制約

**ロックファイル(`simulate.ts`・`analyze.ts`・`montecarlo.ts`)は変更禁止。**
対象は`src/content/blog/sequence-of-returns-risk.md`のみ。他は一切変更しないこと。

## 背景

前回の調査(10回分のMC実行結果)で、記事本文の「約31〜32%」等の幅表現が
実測のばらつき(A:28.9〜33.4%等)に対して狭すぎることが判明した。幅を
広げるのではなく、平均値による単一の代表値に統一し、「モンテカルロは
乱数を使うため実行毎に変動する」という説明を一言添える方針とする
(プランニングチャット側で確定済み)。

## 変更内容(該当箇所を以下の通り置き換え)

1. **試算結果の表**(現在「約31〜32%」「約58〜61%」「約72〜75%」となっている表)を
   以下に変更:
   - A. 通常 → **約31%**
   - B. 退職1年目に中程度の下落 → **約59%**
   - C. 退職1年目に非常に大きな下落 → **約73%**

2. **上記の表の直後**(「退職1年目のリターンだけを変えたところ〜」の文の前)に、
   以下の一文を追加:
   > ※モンテカルロ・シミュレーションは乱数を使うため、実行するたびに結果が数ポイント程度変動します。上記は複数回試算した平均的な値です。

3. **「今回の試算で分かったこと【結論】」ブロック内**の「約31〜32%」「約72〜75%」を、
   それぞれ**「約31%」「約73%」**に変更。

4. **リード文直後のTL;DR(「この記事の結論」)**の「約31%→約73%」の表記は
   既に平均値に近いため、変更不要(このまま)。

上記以外の文言・数値・構成・画像埋め込み箇所は一切変更しないこと。

## 完了報告に含めるべき内容

- 変更diff(該当4箇所)
- `full-verify.js`・`tsc`の再確認結果(記事はコードに影響しないはずだが念のため)

commit・pushは行わず、報告のみでお願いします。この修正が完了すれば、記事は
公開準備完了となる想定です。
