# 実装指示書:記事公開前の最終修正3点

## 対象・制約

**ロックファイル(`simulate.ts`・`analyze.ts`・`montecarlo.ts`)は変更禁止。**
対象は`src/content/blog/sequence-of-returns-risk.md`と`src/data/concerns.ts`
(該当エントリのみ)。他のファイルには一切触れないこと。

## 1. categoryの修正

`src/content/blog/sequence-of-returns-risk.md`のfrontmatter、
`category: "ケーススタディ"`を**`category: "シミュレーター活用"`**に変更して
ください(既存の`prepay-vs-invest`と同じカテゴリに統一するため)。本文・
数値・構成は変更しないこと。

## 2. 生HTMLタグをMarkdown記法に統一

記事本文内の2箇所の`<a href="...">...</a>`タグを、既存記事と同じ
Markdownのリンク記法`[リンクテキスト](URL)`に置き換えてください。
リンクテキスト・URL(UTMパラメータ含む)は一切変更せず、記法だけを
統一してください。変更後、`scripts/check-raw-html-in-blog.js`を再実行し、
警告が解消されていることを確認してください。

## 3. 数値の安定性の再確認

`scripts/sequence-risk-orcan-model-numbers.js`(6,000万円版)を、追加で
3回実行してください(これまでの実行結果と合わせて計5回分のデータになる
想定)。A/B/Cそれぞれの破綻確率の分布(最小値・最大値・平均値)を報告して
ください。

その上で、記事本文に記載している範囲(A:約31〜32%、B:約58〜61%、
C:約72〜75%)が、この5回分のデータの実態と比べて適切な表現か所感を
述べてください。「複数回実行した平均的な傾向として、この範囲が妥当と
言えるか」「範囲を広げる、あるいは"およそ〇割"のような、より緩い表現に
変更した方が実態に即しているか」を判断材料として報告してください。
**記事本文の数値を実際に変更するかどうかは、この報告を受けてプランニング
チャット側で判断するので、今回は変更せず、報告のみで構いません。**

## 完了報告に含めるべき内容

- 上記1・2の変更diff
- `check-raw-html-in-blog.js`の再実行結果(警告解消の確認)
- 5回分のA/B/C実行結果の一覧と、範囲の妥当性についての所感
- `full-verify.js`・`tsc`の再確認結果

commit・pushは行わず、報告のみでお願いします。
