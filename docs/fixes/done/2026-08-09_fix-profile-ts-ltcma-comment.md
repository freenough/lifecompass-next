# 指示ファイル:profile.ts コメント修正(LTCMA改訂時期の誤記訂正)

## 種別
実装指示(投資不要・コメントのみの軽微修正)

## 目的
`src/lib/profile.ts:22`のコメントにある「LTCMAは毎年12月頃に改訂される」という
記述が事実と異なることが判明した(前回タスクの完了報告で発見)。実際の発表時期は
過去実績より10月中旬〜下旬。`reference/asset-classes-ltcma.md`(前回タスクで作成
済み)の内容と整合させる。

## 背景
- 28th(2024年版):2023年10月17日発表
- 29th(2025年版):2024年10月下旬発表
- 30th(2026年版):2025年10月20日発表
- いずれも10月中旬〜下旬であり、「12月頃」は誤り

## 作業内容

### 対象
`src/lib/profile.ts:22`(前回の完了報告で特定された行。念のため作業前に該当行を
再確認し、行番号がずれていないか確認すること)

### 修正内容
現状のコメント:
```
LTCMAは毎年12月頃に改訂されるため、この前提値は年次更新が必要。
```

修正後(以下の趣旨で自然な日本語に調整可):
```
LTCMAは毎年10月中旬〜下旬に改訂されるため、この前提値は年次更新が必要。
詳細は reference/asset-classes-ltcma.md を参照。
```

### 確認事項
- コメント以外のコード(ロジック・値)は一切変更しないこと
- 該当箇所以外に同様の「12月頃」という誤記が`profile.ts`内や他ファイルに
  ないか、念のため`grep`等で確認すること(見つかった場合は修正せず報告のみ)

## スコープ外(やらないこと)
- ASSET_CLASSESの数値自体の変更
- `reference/asset-classes-ltcma.md`の再編集(前回タスクで作成済み)
- Git commit/push(このタスクと前回の`reference/asset-classes-ltcma.md`作成分を
  まとめて、プランニングチャットでの承認後に一括で行う)

## 完了報告に含めるべき事項
- 修正箇所(行番号・修正前後の文言)
- 他に同様の誤記がなかったかのgrep結果
- `full-verify.js`は今回対象外(コメントのみのため)だが、念のため
  `tsc --noEmit`でエラーが出ないことの確認結果
