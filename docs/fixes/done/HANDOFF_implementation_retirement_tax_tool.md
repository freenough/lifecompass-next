# 引き継ぎ:第5弾ツール(退職金手取り計算)実装フェーズ着手

## 前提

Product Spec策定完了。`product_spec_retirement_tax_tool.md`を参照のこと。
本ファイルと合わせて、以下2つの実装ドラフトを同梱:
- `retirement.ts` → `src/lib/tax/retirement.ts` として配置する想定
- `retirement.test.ts` → `src/lib/tax/retirement.test.ts` として配置する想定
  (テストフレームワークのimport文は既存プロジェクトの構成に合わせて調整)

これらはチャット側(コードベース未接続)で作成したドラフトのため、
**既存プロジェクトへの実際の組み込み・動作確認はClaude Code側で行うこと。**

## 最優先:前回の積み残し

- `retirementTaxCalc()`(本体・`helpers.ts`)の80万円下限修正が
  作業ツリーに残ったままの可能性がある。まずコミット・デプロイ状況を確認する

## 実装時にやること

1. `retirement.ts`を`src/lib/tax/`配下に配置し、プロジェクトのimport規約
   (パスエイリアス等)に合わせて調整
2. `retirement.test.ts`を配置し、実際のテストランナーで実行、TODO部分
   (国税庁公開計算例との突合、田中誠シリーズとの整合性チェック)を埋める
3. Product Spec 2節(入力項目)・4節(結果表示)・5節(差異明示)に沿って
   ツールUIを実装
   - 「退職手当等の種類」はユーザーに見せない(勤続年数+役員チェックから自動判定)
   - 役員チェックボックスは勤続年数≤5年の場合のみ表示
   - 障害者特例は「詳細設定」に折りたたみ
   - 結果は内訳(控除→課税退職所得→所得税→住民税→手取り)を常時表示
   - 「計算根拠を見る」開閉式セクションで本体との差異を明示(20.315%という
     具体数値は書かない、Spec5節の文言案を使用)
4. 既存Toolsの命名・ルーティング規約(積立額逆算・目標資産到達年齢・
   複利計算機・年金繰上げ繰下げ)に合わせてツールページを追加
5. 実装・デプロイ完了後、methodologyページへの導線追記
   (引き継ぎメモ`NEXT_CHAT_PROMPT_Tools_06.md`に文言案あり)を行う

## 検証観点(再掲、Spec7節)

- `calcRetirementDeduction()`は本体`retirementTaxCalc()`(80万円下限修正済み)
  と一致することを確認(独立実装のため、突合テストで担保する)
- 端数処理(課税退職所得1,000円未満切り捨て/所得税1円未満切り捨て/
  住民税は市民税・県民税それぞれ100円未満切り捨て後に合算)が
  正しく実装されているか
- 控除額>退職金の境界ケースで税額が0円になるか
