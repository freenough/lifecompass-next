前回の実装(未達成時の達成率化)はそのまま活かしてください。追加で以下2点を対応してください。

達成時の補足も「資産≥支出×25」から「達成率 ◯◯%」に変更してください。ただし計算の基準スナップショットはretSnap(p.retAge時点)ではなく、a.fA(FIRE達成年齢)時点のスナップショットを使ってください。baseSnaps.find(s => s.age === p.retAge)と同じパターンで、age === a.fAに差し替えて取得できます。計算式は未達成時と同じ(資産 ÷ (支出×25) × 100、四捨五入)です。
?マークの説明文を更新してください。現在の文言を、「FIRE達成率は、達成時はFIRE達成年齢時点、未達成時は退職予定年齢時点の資産が、「年間支出×25」の何%に達しているかを表します。」という一文に置き換えてください(達成時・未達成時で基準年齢が異なることを明記する必要があります)。