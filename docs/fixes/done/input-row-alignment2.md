input-row-alignment 追加修正。右寄せ後、単位の文字数差(万円/年・万円・%・歳・年)により

入力欄の開始位置が行ごとにズレて凸凹に見える問題の修正。



方針: 単位を固定幅の列にする。

1\. Field/DisplayFieldの単位<span>に固定幅(最長単位「万円/年」が折り返し・省略なしで

&#x20;  収まる幅。想定 w-14 前後、実測で決定)+ text-left を設定。

&#x20;  全行でクラスタ(入力w-24＋単位固定幅)のトータル幅が同一になり、

&#x20;  右端と入力欄左端の両方が揃うことを確認する

2\. 単位のない行(iDeCo受取方式ドロップダウンなど)は、同じ固定幅の空スペーサーを

&#x20;  置くか、要素のトータル幅を他行クラスタと一致させて左右端を揃える

3\. RateField: 入力w-16＋%＋MiniToggleのクラスタも、%部分を同じ固定幅列にした上で

&#x20;  トータル幅が他行クラスタと一致するようw-16側で調整する(必要ならw-16の値を変更可。

&#x20;  ここのトータル幅一致が本来の設計意図)

4\. LifeEventTimelineの単位(歳・年・万円/年・%)も同じ固定幅ルールを適用する

5\. 単位の固定幅の値は1箇所(定数または共通クラス)で定義し、各所ハードコードにしない



確認方法: モバイル幅(375px)とデスクトップ幅で、家計・資産・運用方針・タイムラインの

全A分類行について「入力欄の左端が縦に一直線」「単位列の左端が縦に一直線」の

2点を目視確認し、スクリーンショットを完了報告に含める。

6-A/6-B スナップショット比較も再実施(KPI値完全一致が期待値)。

