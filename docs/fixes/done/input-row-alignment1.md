input-row-alignment ステップ0の報告を確認した。以下の方針でステップ1の実装をGOする。

1. Field/DisplayField/RateFieldの行コンテナに justify-between を追加(共通クラスのまま。
   lg:分岐は作らず、デスクトップにも同一変更が適用されることを許容する)
2. RateFieldは入力+%+MiniToggleを1つのクラスタ(flex items-center gap-1)にまとめ直し、
   3要素ごと右寄せする。入力欄のw-16は意図的な設計(クラスタのトータル幅を他行の
   入力+単位と揃えるため)なので変更しない
3. LifeEventTimeline: 直書き構造は維持し、justify-between追加＋幅統一のみ行う。
   幅は 開始年齢 w-16→w-24 / 期間 w-16→w-24 / 返済年数 w-16→w-24 / 金利 w-20→w-24
4. 一時金割合(SimulatorForm.tsx:471-489): 直書き維持、w-20→w-24、justify-between適用
5. iDeCo受取方式ドロップダウン(本人・配偶者とも): 他のA分類行と同様に右寄せし、
   selectの幅をw-24に固定する。選択肢テキストがw-24からはみ出す・省略される場合は
   実装を止めて報告
6. B分類(ポートフォリオ行)・C分類(種別ドロップダウン行・名称入力行・チェックボックス行)・
   ボタン行は変更しない

完了報告に含めるもの:
- 変更ファイル一覧と変更内容の要約
- 6-A/6-B スナップショット比較(レイアウトのみの変更のためKPI値は完全一致が期待値。
  差分が出たら実装を止めて即報告)
- モバイル幅(375px前後)の各セクション確認結果(利回り/MC設定のトグル位置と右端揃い、
  タイムライン全項目の幅統一、iDeCo受取方式の表示崩れ有無を含む)
- デスクトップ幅(lg以上、サイドバー320px)の各セクション確認結果

完了後、docs/fixes/active/input-row-alignment.md を docs/fixes/done/ へ移動すること。