# 指示ファイル:記事へのアフィリエイトセクション追加+お悩み一覧への反映

**作成日:** 2026-08-10
**フェーズ:** 実装
**担当リポジトリ:** lifecompass-next
**前提:** `src/content/blog/retirement-ideco-timing.md`が配置済み・コミット待ちの状態であることを前提とする。まだコミットしていなければ、本タスクの変更も含めて一緒にコミットしてよい。コミット済みの場合は、本タスクの差分を追加コミットする。

---

## タスクI:記事への[PR]表記+アフィリエイトセクション追加

添付の更新版`retirement-ideco-timing.md`(このチャットから納品)に差し替えるか、以下の2箇所の差分を既存の配置済みファイルに直接適用すること。

### 差分1:記事冒頭への[PR]表記追加

H1タイトルの直後、アイキャッチ画像の前に以下を追加:

```
[PR] 本記事にはアフィリエイト広告を含みます。紹介する内容は、シミュレーターの計算前提や比較検討の一例であり、特定の商品・サービスを推奨するものではありません。
```

(`ideco-nisa.md`の冒頭表記と同一の文言・位置にすること)

### 差分2:FAQセクションと「まとめ」セクションの間に新規セクションを追加

```markdown
## 受け取った後の資金管理も、あわせて検討を

退職金やiDeCoの一時金は、まとまった金額を一度に受け取ることになります。受け取った資金をそのまま普通預金に置いておくか、一部を運用に回すかは、退職後の資産計画を考えるうえで欠かせない検討ポイントです。

[PR] 運用を検討する場合は、証券口座の準備が必要になります。[松井証券 公式サイトはこちら](リンクURL)
```

リンクURLは、`ideco-nisa.md`または他の既存記事で使われているA8.net経由のMatsui Securitiesアフィリエイトリンクと同一のものを使用すること(新規リンクを発行する必要はない。既存記事から実際のURLをそのままコピーすること)。

### 確認事項

- ads.txt・A8.net関連の既存設定に変更は不要(既存の提携をそのまま使うため)
- リンクにnofollow等の属性が既存のアフィリエイトリンクで使われている場合は、同じ属性を今回のリンクにも適用すること

---

## タスクJ:お悩み一覧(concerns.ts)への新規concern card追加

`src/data/concerns.ts`に、新規concern cardを追加する。既存のcardの構造(フィールド名・型)をそのまま踏襲し、以下の内容で作成すること。

### 追加内容(案。既存のconcern card構造に合わせてフィールド名等は調整してよい)

- **問いかけ文(タイトル相当):** 「退職金とiDeCo、いつ受け取るのが得?」
- **説明文:** 退職金とiDeCoを近い時期に受け取ると、退職所得控除が重複して調整される場合があります。19年ルール・10年ルール(2026年改正後)を踏まえて、受給タイミングによる手取り額の違いを試算できます。
- **toolUrl(または相当のフィールド):** `/asset-simulator/tools/retirement-ideco-timing`
- **articleUrl(または相当のフィールド):** `/asset-simulator/blog/retirement-ideco-timing`
- **カテゴリ・タグ等:** 既存のconcerns.tsの分類軸(`ideco`関連の他のconcern cardがあれば、それに近い分類)に合わせること

### 確認事項

- 既存の`ideco-withdrawal`関連のconcern cardと表示位置・並び順が近くなるよう配置することを検討する(既存の並び順のルールがあれば、それに従う)
- concern cardクリック時のGA4カスタムイベント(concern card clicksの計測)が、新規追加分にも正しく発火することを確認する

---

## 厳守事項

- `simulate.ts` / `analyze.ts`には触れないこと
- タスクIのアフィリエイトリンクURLは、既存記事からコピーした実在するリンクを使うこと(架空のURLを作らないこと)
- 完了報告には、コピー元にした既存記事名と、実際に使用したリンクURL(の存在確認結果)を明記すること
- `full-verify.js`・`tsc --noEmit`を確認すること
- **コミット/プッシュ前に、完了報告をこのチャットに提出し承認を得ること**
- `docs/fixes/active/`フォルダ自体をrmdirしないこと
