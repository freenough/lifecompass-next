// BudouXで区切った結果について、途中で区切らない用語のリスト（impl_budoux_phrase_break.md 2節）。
// 区切りの位置がこの用語の途中にある場合だけ、その区切りを取り除く（隣り合う文節を結合する）。
// 用語の直前・直後の区切りはそのまま残す。
//
// 運用ルール:
// - 実測で「語の途中での区切り」が確認された語だけを追加する。推測では追加しない。
// - 追加したら、scripts/verify-phrase-break.js にその語のケースを足す。
//
// 長い用語から先に判定する（segment.tsで長さ順に並べ替えるため、ここでの並び順は問わない）。
export const PROTECTED_TERMS: readonly string[] = [
  '一人法人', // 実測: 「一人／法人の」「一人／法人を」などサイトの中心的な用語が分けられていた
  '法人', // 実測: 「一人／法／人って、」で語の途中が区切られていた
];
