# SPEC_TEMPLATE.md

大きな機能（SP-ACCOUNT、CUSTOM-ASSET等）を追加するときに使う雛形。
Claude Codeで使う場合は、まずこのファイルをコピーして `SPEC.md` を作り、
以下のような形でインタビューを依頼してから埋めていく。

```
SP-ACCOUNT（配偶者口座の独立化）を実装したい。AskUserQuestionツールを使って詳しく
インタビューしてほしい。技術的な実装方法・UI/UX・エッジケース・トレードオフについて聞いて、
当たり前の質問は省いて、見落としていそうな難しい部分を掘ってほしい。
聞き終わったら、このSPEC_TEMPLATE.mdの構成でSPEC.mdに書き出して。
```

埋め終えたSPEC.mdは新しいセッションで実装に使う（インタビューの試行錯誤がコンテキストに
残らない、実装に集中したクリーンな状態で始められる）。

---

## 機能名

## 背景・目的
なぜこの機能が必要か。ROADMAP.mdでの優先度・KENZOからの要望背景。

## スコープ
この機能に含むもの／含まないもの（含まないものを明記するのが重要）。

## 影響するファイル・関数
- 既存の `simulate()` / `analyze()` のどの部分に手を入れるか
- 新規に追加するファイル・コンポーネント

## データ構造の変更
既存の `YearSnapshot` 等にどのフィールドを追加・変更するか。
既存の確定値（reference/simulation_fixtures.md）に影響が出る変更かどうかを明記する。

## UI/UX
- 入力フォームの変更点
- 既存のラベル左・入力右の原則を維持するか、変更が必要か

## エッジケース
- 想定される境界値・異常値（例：配偶者の年齢が本人より上の場合、退職年齢が現在年齢より前の場合）
- 既存4シリーズ（田中・山本・中村・佐々木）のいずれかと矛盾しないか

## 検証方法
- この機能を追加した後、reference/simulation_fixtures.md の既存シナリオが変わらず一致することの確認方法
- 新機能自体の正しさをどう検証するか（手計算ではなく、シミュレーター実行結果との比較で）

## スコープ外・将来検討
今回やらないこと、次の機能として持ち越すこと。
