# LifeCompass（Next.js移植版）

## プロジェクト概要
元はSTEP35まで単一HTMLファイルで開発したFIRE/退職資産シミュレーター。
旧版: https://freenough.github.io/lifecompass/
本リポジトリはこれをNext.js（TypeScript）へ移植する先。
note.com/freenoughで連載中の4シリーズ小説（田中誠／山本恒一／中村夫婦／佐々木誠一）と連動しており、
シミュレーターの計算結果が記事の数値の土台になっている。

## 移植フェーズの大原則
- ロジック移植とUI刷新は同時にやらない。先に計算結果が旧HTML版と一致することを確認し、その後でデザインに着手する。
- 機能追加（SP-ACCOUNT、CUSTOM-ASSET等）は移植完了後。移植中は機能セットを凍結する。
- 移植中に挙動の違いを見つけたら、まず「移植のミスか、旧版自体の仕様か」を区別する。判断に迷ったらKENZOに確認する。

## アーキテクチャ（旧HTML版から継承する不変ルール）
- コア関数は `simulate(p, events, strategy, shockOverrides?)` と `analyze(snaps, p)` の2つのみ。UI層はこれだけを呼ぶ。
- 年末積立方式：前年末残高を1年運用後、年末に積立を加算する（保守的推定）。
- 退職年齢の解釈：`age < retAge` の間だけ積立を行う。退職年齢当年は収入・積立あり。
- 取崩は比例取崩方式（NISA・現金を残高比率で按分）。現金は積立期に運用しない。
- FIREライン = `baseExp × 25`（インフレ調整済み恒久生活費）。住宅ローン・教育費等のイベント支出はFIRE判定に無関係。
- FIRE達成年齢には3つの定義がある：①最低限（積立期最終値 >= 支出×25）②最速（取崩期に90歳まで資産>0）③安心（取崩期に生涯資産 >= 支出×25）。

## 検証ルール（最重要）
- 数値を独立計算しない。`reference/simulation_fixtures.md` にある確定値、または旧HTML版から再出力したCSVと必ず突き合わせる。
- 固定利回りシナリオは完全一致を求める。モンテカルロは乱数を含むため、破綻率等の統計値が許容範囲内かで判定する。
- 移植中の回帰確認は `verify-migration` スキルを使う。可能ならStop Hookで強制する（`.claude/settings.json`参照）。

## よくある間違い（旧HTML版で実際に発生したもの）
- `age >= retAge` から積立停止としてしまう（正しくは `age < retAge` まで積立）
- NISAのみから取崩としてしまう（正しくは比例取崩）
- 現金に利回りを適用してしまう（積立期のcashは運用なし）
- 積立期に口座ごとに別の利回りを設定してしまう（積立期は全口座共通でrW、取崩期は全口座共通でrR+shock）
- 取崩期の特定口座は課税（約20%）が乗るため、スクリプト上の想定破綻率より実機の方が高くなる場合がある

## 開発ワークフロー
- 複数ファイルに影響する変更、設計の見通しが立たない変更はPlan Modeで提案を確認してからGOする。一文で言える小さい修正は直接実装してよい。
- 大きな機能を追加するときは `SPEC_TEMPLATE.md` の型でインタビューを受け、`SPEC.md` を書き出してから、新規セッションで実装する。
- コミットメッセージは変更内容を具体的に書く。

## ディレクトリ構成（想定）
- `reference/` — 回帰テスト用の確定パラメータ・確定数値
- `.claude/skills/` — 再利用ワークフロー（verify-migration等）
- `.claude/settings.json` — Hooksの設定
- `scripts/` — 検証スクリプト
- `SPEC_TEMPLATE.md` — 大型機能のスペック雛形

@reference/simulation_fixtures.md
