# LifeCompass（Next.js移植版）

## プロジェクト概要
元はSTEP35まで単一HTMLファイルで開発したFIRE/退職資産シミュレーター。
旧版: https://freenough.github.io/lifecompass/
本リポジトリはこれをNext.js（TypeScript）へ移植する先。
note.com/freenoughで連載中の4シリーズ小説（田中誠／山本恒一／中村夫婦／佐々木誠一）と連動しており、
シミュレーターの計算結果が記事の数値の土台になっている。

## 移植フェーズの大原則
- ロジック移植とUI刷新は同時にやらない。先に計算結果が旧HTML版と一致することを確認し、その後でデザインに着手する。
  → 2026-06-22 完了。全4シリーズ・70チェックポイント（固定値＋MC）PASS。以降はUI刷新フェーズ（Phase 3）へ移行。
- 機能追加（SP-ACCOUNT、CUSTOM-ASSET等）は移植完了後。移植中は機能セットを凍結する。
- 移植中に挙動の違いを見つけたら、まず「移植のミスか、旧版自体の仕様か」を区別する。判断に迷ったらKENZOに確認する。

## アーキテクチャ（旧HTML版から継承する不変ルール）
- コア関数は `simulate(p, events, strategy, shockZOverrides?)` と `analyze(snaps, p)` の2つのみ。UI層はこれだけを呼ぶ。
- 年末積立方式：前年末残高を1年運用後、年末に積立を加算する（保守的推定）。
- 退職年齢の解釈：`age < retAge` の間だけ積立を行う。退職年齢当年は収入・積立あり。
- 取崩は比例取崩方式（NISA・現金を残高比率で按分）。現金は積立期に運用しない。
- FIREライン = `baseExp × 25`（インフレ調整済み恒久生活費）。住宅ローン・教育費等のイベント支出はFIRE判定に無関係。
- FIRE達成年齢 = 総資産が「生活費×25」を上回り、以後生涯その水準を一度も下回らない最初の年齢（生涯維持条件。瞬間的な到達ではない）。

### モンテカルロのμ/σ設計（2026-07-04確定・旧HTML版STEP31〜35から継承）
- **μ（期待リターン）は口座ごとの個別値**。`simulate()`は口座ごとの残高に対してその口座自身の
  rW/rRを個別に複利適用する（`p.acct.nisa.rW`等）。全口座を1本にまとめたμは存在せず、
  「全口座集計」のμはUI表示専用（`calcAggregateMu`）で、シミュレーション結果には一切使われない。
- **σ（shock）は口座横断で単一の値、相関=1を仮定する設計**。`montecarlo.ts`は年ごとに
  標準正規乱数（Zスコア）を1つだけ生成し、`simulate()`内でその年の口座別σ×その時点の
  実残高（`nisa/ideco/tax` + 配偶者分）を加重平均した`sigmaT`を掛けて、**同一のshockを
  3口座すべてに加算**する（`nisaRate = rW + shock`、`idecoRate`・`taxRate`も同様）。
  「市場全体が一緒に動く」という意図的な簡略化であり、変更対象ではない。
- **全口座集計のμ・σの重みは「資産配分の入力有無」ではなく実際の残高・積立額**
  （`getAggregateWeights`: 積立期は残高＋積立額、取崩期は残高のみ。①現在のPFに金額入力が
  あればそれを優先）。残高・積立額が0円の口座は、資産配分を入力しても重み0のままになる。
- **重みは年ごとに動的に再計算される**（`AccountConfig.sigmaW/sigmaR` × `SimParams.mcStdDynamic`/
  `mcStdRDynamic`）。「PF計算値を使う」がONの口座だけが動的モード対象。手動入力（OFF）の
  口座は従来通りmcStd/mcStdR固定値のまま（`scripts/full-verify.js`等、SimParamsを直接組み立てる
  呼び出し元はこれらのフィールドを設定しないため、常に静的モード＝既存の挙動を維持する）。
- σの残高加重平均は近似ではない。相関=1を仮定する設計のもとでは、
  σ(aX+bY) = |a|σX + |b|σY（ρ=1のとき）が成り立つため、残高加重平均が数学的に正確な合成方法。
- **`calcPortfolioMetrics`（LTCMA相関行列・SIGMA-AUTO）は「口座内」で複数資産クラスを
  合成する場合にのみ使ってよい。「口座間」の合成には絶対に使ってはならない**
  （2026-07-04にこの誤用がバグとして発見・修正された：`calcAggregatedSigma`が口座をまたいで
  資産クラスをマージしてから`calcPortfolioMetrics`に通しており、口座間にも相関行列（ρ<1）が
  適用されてしまっていた）。現在のモンテカルロは全口座が同一のショックを受ける（相関=1）
  というモデルである。このモデルでは、ポートフォリオσは各口座σの残高加重線形和が
  数学的な厳密解となる。相関行列を用いて算出したより小さいσを用いると、「資産は完全に
  同じ方向へ動く」というシミュレーションモデルと、「完全には同じ動きをしないことで分散
  効果が得られる」というσの前提が矛盾し、結果としてリスクを過小評価することになる。
  将来、資産クラスごとに相関を反映した独立したショックを生成するモンテカルロへ拡張する
  場合に初めて、共分散行列を用いたσが理論的に整合する。

## 検証ルール（最重要）
- 数値を独立計算しない。`reference/simulation_fixtures.md` にある確定値、または旧HTML版から再出力したCSVと必ず突き合わせる。
- 固定利回りシナリオは完全一致を求める。モンテカルロは乱数を含むため、破綻率等の統計値が許容範囲内かで判定する。
- 移植中の回帰確認は `verify-migration` スキルを使う。可能ならStop Hookで強制する（`.claude/settings.json`参照）。

## よくある間違い（旧HTML版で実際に発生したもの）
- `age >= retAge` から積立停止としてしまう（正しくは `age < retAge` まで積立）
- NISAのみから取崩としてしまう（正しくは比例取崩）
- 現金に利回りを適用してしまう（積立期のcashは運用なし）
- 積立期に口座ごとに別の利回りを設定してしまう（積立期は全口座共通でrW、取崩期は全口座共通でrR+shock）
- 取崩期の特定口座は課税（約20%）が乗るため、スクリプト上の想定破綻率より実機の方が高くなる場合がある

## 開発ワークフロー
- 複数ファイルに影響する変更、設計の見通しが立たない変更はPlan Modeで提案を確認してからGOする。一文で言える小さい修正は直接実装してよい。
- 大きな機能を追加するときは `SPEC_TEMPLATE.md` の型でインタビューを受け、`SPEC.md` を書き出してから、新規セッションで実装する。
- コミットメッセージは変更内容を具体的に書く。

## ディレクトリ構成（想定）
- `reference/` — 回帰テスト用の確定パラメータ・確定数値
- `.claude/skills/` — 再利用ワークフロー（verify-migration等）
- `.claude/settings.json` — Hooksの設定
- `scripts/` — 検証スクリプト
- `SPEC_TEMPLATE.md` — 大型機能のスペック雛形

## UI刷新フェーズの設計原則（Phase 3）
- カラー：白主体・青系アクセント・薄い灰色系の3色以内
- グラフ：Recharts採用（Chart.jsへのフォールバックはSPEC.md参照）
- モバイル：DOM複製なし・全機能対応・ブレークポイント600px
- オンボーディング：Welcomeモーダル廃止。初回アクセス時はサンプルプロファイルを自動読み込みしてグラフを表示
- ロジック不変：simulate()/analyze()のシグネチャ変更禁止。full-verify.jsが常にPASSであること

## 設計原則(2026-08-03追加)

### category軸とtopics軸は別役割・安易に統合しない
- `category`:記事の制作形式を表すブランド軸(表示用バッジ)
- `topics`:記事の主題を表すスコアリング・フィルタ用の軸
- 見た目が似ていても、統合・置換すると意味の異なる2つの分類が壊れるため注意

### 表示用途への転用はfrontmatter非変更が基本
- スコアリング専用データ(frontmatterのtopics等)に表示用の意味を持たせたい場合、
  frontmatter自体は変更せず、コード側のマッピングテーブル(例:`blogTopics.ts`の
  `TOPIC_GROUPS`)で対応する

### エンティティ属性は単体で切り出さない
- 「このエンティティのこの属性だけ共通化する」は避ける(同じエンティティのメタデータが
  複数箇所に分散する構造を新たに生むため)
- 共有ファイルに切り出す単位は「エンティティ全体」(例:ツールなら`TOOLS`配列そのもの
  を`toolMetadata.ts`に集約、属性だけの抜き出しはしない)

### 表記統一は定数化までワンセットで行う
- 同じ文言が複数箇所にハードコードされている問題を見つけたら、値を揃えるだけでなく
  定数化(例:`siteCopy.ts`)まで行い再発防止する
- ただし対象範囲は実害が確認された箇所に限定し、「ついでに全体を集約する」提案には
  慎重に線を引く(スコープの肥大化を避ける)

### retirement-taxツールは意図的な別実装
- `retirement-tax`ツールは本体シミュレーター(`simulate.ts`/`analyze.ts`)とは別の
  税額計算ロジック(`src/lib/tax/retirement.ts`)を使用している
- これはバグや統合漏れではなく意図的な設計判断。本体ロジックへの統合を提案・実施しない
- 記事内でこの実装差について言及しない方針(コンテンツ制作上の注意点)

## セッション管理

### コミットルール
- セッション開始時: `git add . && git commit -m "before session[N]"` を必ず実行
- セッション終了時: `git add . && git commit -m "session[N]完了: [実装内容]"` を実行
- 実装前にコミットがない場合は、コミットしてから作業開始すること


@reference/simulation_fixtures.md
